import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: ['olhardigital.com.br', 'img.freepik.com', 'i.pravatar.cc', 'picsum.photos'],
  },
};

export default nextConfig;
